<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <style>
    :root {
      --bg-gradient: linear-gradient(135deg, #0d1b2a, #1b263b, #415a77);
      --glass-border: rgba(255, 255, 255, 0.15);
      --accent-blue: #4cc9f0;
      --accent-green: #4fe8b0;
      --accent-purple: #9d4edd;
      --text-light: #e5e9f0;
      --text-muted: #9aa3b2;
    }

    body {
      margin: 0;
      padding: 0;
      background: var(--bg-gradient);
      font-family: "Inter", Arial, sans-serif;
      color: var(--text-light);
    }

    /* MAIN LAYOUT */
    .profile-container {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 40px 20px;
    }

    .back-btn {
      position: absolute;
      top: 20px;
      left: 20px;
    }

    /* CARD */
    .profile-card {
      width: 100%;
      max-width: 520px;
      padding: 40px;
      background: rgba(255, 255, 255, 0.06);
      border: 1px solid var(--glass-border);
      border-radius: 20px;
      backdrop-filter: blur(20px);
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.4);
      animation: fadeIn 0.4s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .title {
      text-align: center;
      font-size: 2.2rem;
      font-weight: 700;
      color: var(--accent-blue);
      text-shadow: 0 0 15px #4cc9f099;
      margin-bottom: 30px;
    }

    /* BUTTONS */
    .btn {
      padding: 12px 22px;
      border-radius: 12px;
      font-weight: 600;
      text-decoration: none;
      display: inline-block;
      border: none;
      cursor: pointer;
    }
    .btn-primary {
      background: rgba(76, 201, 240, 0.2);
      color: white;
      border: 1px solid var(--accent-blue);
      box-shadow: 0 0 10px rgba(76, 201, 240, 0.3);
    }
    .btn-secondary {
      background: rgba(157, 78, 221, 0.2);
      color: white;
      border: 1px solid var(--accent-purple);
      box-shadow: 0 0 10px rgba(157, 78, 221, 0.3);
      margin-bottom: 20px;
    }

    /* AVATAR SECTION */
    .avatar-box {
      text-align: center;
      margin-bottom: 25px;
    }

    .avatar-preview {
      width: 140px;
      height: 140px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid rgba(76, 201, 240, 0.5);
      box-shadow: 0 0 20px rgba(76, 201, 240, 0.4);
      margin-bottom: 12px;
    }

    .file-input {
      margin-top: 12px;
    }

    /* FORM FIELDS */
    .form-group {
      margin-top: 18px;
    }

    label {
      font-weight: 600;
      color: var(--accent-green);
      margin-bottom: 5px;
      display: block;
    }

    .input-box {
      width: 100%;
      padding: 14px;
      background: rgba(255, 255, 255, 0.08);
      border: 1px solid var(--glass-border);
      border-radius: 12px;
      color: var(--text-light);
      font-size: 1rem;
      backdrop-filter: blur(10px);
    }

  </style>
</head>

<body>

  <div class="profile-container">

    <div class="back-btn">
      <a href="/users/dashboard" class="btn btn-primary">🏠 Home</a>
    </div>

    <div class="profile-card">

      <h1 class="title">✏️ Edit Profile</h1>

      <a href="/users/profile" class="btn btn-secondary">← Back to Profile</a>

      <form action="/users/edit-user/<%= user.userId %>" method="POST" enctype="multipart/form-data">

        <!-- AVATAR -->
        <div class="avatar-box">
          <% const avatar = user.profileImage || "/uploads/default-avatar.png"; %>

          <img src="<%= avatar %>" id="avatarPreview" class="avatar-preview">

          <div>
            <input type="file" name="avatar" id="avatarInput" accept="image/*" class="file-input" />
            <p style="color: var(--text-muted); font-size: 13px; margin-top: 6px;">
              Max 5MB • JPG / PNG / GIF
            </p>
          </div>
        </div>

        <!-- NAME -->
        <div class="form-group">
          <label>First Name</label>
          <input class="input-box" type="text" name="firstName" value="<%= user.firstName %>" required>
        </div>

        <div class="form-group">
          <label>Last Name</label>
          <input class="input-box" type="text" name="lastName" value="<%= user.lastName %>" required>
        </div>

        <div class="form-group">
          <label>Email</label>
          <input class="input-box" type="email" value="<%= user.email %>" readonly>
        </div>

        <button class="btn btn-primary" type="submit" style="margin-top: 25px; width: 100%;">Save Changes</button>
      </form>

    </div>

  </div>

  <script>
    // Avatar Preview
    const fileInput = document.getElementById("avatarInput");
    const preview = document.getElementById("avatarPreview");

    fileInput.addEventListener("change", () => {
      const file = fileInput.files[0];
      if (!file) return;

      const reader = new FileReader();
      reader.onload = e => preview.src = e.target.result;
      reader.readAsDataURL(file);
    });
  </script>

</body>
</html>
