<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="profile-body">
  <div class="profile-container">
    <div class="profile-card">
      <h1 class="title">✏️ Edit Profile</h1>

      <!-- Back to Dashboard Link -->
      <a href="/users/dashboard" class="btn btn-secondary back-link">
        ← Back to Dashboard
      </a>

      <form class="profile-form" action="/users/edit-user/<%= user.userId %>" method="POST">
        <div class="form-group">
          <label for="firstName">First Name</label>
          <input type="text" name="firstName" id="firstName" value="<%= user.firstName %>" placeholder="Enter first name" required class="input-box">
        </div>

        <div class="form-group">
          <label for="lastName">Last Name</label>
          <input type="text" name="lastName" id="lastName" value="<%= user.lastName %>" placeholder="Enter last name" required class="input-box">
        </div>

        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" name="email" id="email" value="<%= user.email %>" class="input-box" readonly>
        </div>

        <button type="submit" class="btn btn-primary" style="margin-top: 20px;">Update Profile</button>

      </form>
    </div>
  </div>
</body>
</html>
